# Copy this to .env and fill values
DISCORD_TOKEN=
PREFIX=!
# Provide Firebase credentials either as a path or a base64-encoded JSON string.
# Recommended for local dev: set the path to the downloaded JSON file:
# GOOGLE_APPLICATION_CREDENTIALS=C:\path\to\serviceAccountKey.json
# Or (recommended for CI / single-line envs): set the base64 JSON below
# GOOGLE_CREDENTIALS_B64=<base64-encoded-service-account-json>
FIREBASE_PROJECT_ID=

# Optional: If you plan to verify interaction requests or register slash commands
# you can set PUBLIC_KEY and CLIENT_ID, but they are not required for simple
# message-based prefix commands. Leave them out unless you need them.
# CLIENT_ID=
# PUBLIC_KEY=
